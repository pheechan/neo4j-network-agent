# 📚 คู่มือถ่ายทอดความรู้ - Neo4j Network Agent

## 🎯 ภาพรวมโครงการ

**STelligence Network Agent** คือแชทบอทตอบคำถามภาษาไทยที่ค้นหาข้อมูลจากฐานข้อมูล Neo4j Knowledge Graph ซึ่งเก็บข้อมูลเกี่ยวกับข้าราชการระดับสูง ตำแหน่ง กระทรวง และความสัมพันธ์ระหว่างกัน

### ฟีเจอร์หลัก
- 🇹🇭 รองรับทั้งภาษาไทยและอังกฤษ
- 🔍 ค้นหาแบบ Vector Search + Graph Traversal (วิธีผสม)
- 💬 หน้าตาแบบ ChatGPT พร้อมระบบเก็บบทสนทนา
- ✏️ แก้ไขข้อความและสร้างคำตอบใหม่ได้
- 🌐 ค้นหาเครือข่าย Stelligence

---

## 📂 ไฟล์ที่ต้องแชร์ให้เพื่อนร่วมงาน

### **ไฟล์สำคัญ (ต้องแชร์)**
1. **`streamlit_app.py`** - ไฟล์หลักของแอปพลิเคชัน (1,350+ บรรทัด)
2. **`requirements.txt`** - รายการ Python libraries ที่ต้องติดตั้ง
3. **`.env.example`** หรือเอกสารตัวแปร environment
4. **`TEST_CASES.md`** - ชุดทดสอบ 34 ข้อสำหรับตรวจสอบคุณภาพ
5. **`README.md`** - คำแนะนำติดตั้งโครงการ

### **ไฟล์ Configuration**
6. **`.streamlit/config.toml`** - การตั้งค่า Streamlit
7. **โฟลเดอร์ `Config/`** - การตั้งค่า Neo4j และ LLM
   - `neo4j.py`
   - `llm.py`
   - `aura_neo4j.py`

### **ไฟล์โค้ดหลัก**
8. **`KG/VectorSearchDirect.py`** - ระบบค้นหาแบบ Vector + Graph
9. **โฟลเดอร์ `Graph/`** - ตรรกะการประมวลผลกราฟ
   - `Tool/Tools.py`
   - `Tool/ToolExecutor.py`
   - `Prompt/prompts.py`

### **เอกสารประกอบ**
10. **`ARCHITECTURE.md`** - สถาปัตยกรรมระบบ
11. **`EMBEDDINGS_SETUP.md`** - คู่มือสร้าง Vector Index
12. **`SETUP_VECTOR_SEARCH.md`** - การตั้งค่าระบบค้นหา
13. **ไฟล์นี้: `KNOWLEDGE_TRANSFER.md`** และ **`KNOWLEDGE_TRANSFER_TH.md`**

---

## 🏗️ สถาปัตยกรรมระบบ

```
┌─────────────────────────────────────────────────────────┐
│                    หน้าตา Streamlit                      │
│  (streamlit_app.py)                                     │
│  - อินเทอร์เฟซแชทพร้อมเก็บบทสนทนา                        │
│  - ปุ่มแก้ไข/สร้างคำตอบใหม่                              │
│  - จัดการ Session State                                 │
└───────────────────┬─────────────────────────────────────┘
                    │
                    ▼
┌─────────────────────────────────────────────────────────┐
│              ชั้นประมวลผลคำถาม                           │
│  - รับคำถามจากผู้ใช้ → แปลงเป็นคำค้นหา Vector          │
│  - ตรวจจับเครือข่าย Stelligence                         │
│  - สร้าง Context                                        │
└───────────────────┬─────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        ▼                       ▼
┌──────────────────┐    ┌──────────────────┐
│  Vector Search   │    │ Graph Database   │
│  (HuggingFace)   │    │    (Neo4j)       │
│  - Embeddings    │    │  - Nodes         │
│  - Similarity    │    │  - Relationships │
└──────────────────┘    └──────────────────┘
        │                       │
        └───────────┬───────────┘
                    ▼
┌─────────────────────────────────────────────────────────┐
│            รวบรวม Context                                │
│  - รวมผลลัพธ์จาก Vector Search + Graph                 │
│  - จัดรูปแบบให้ LLM เข้าใจ                               │
└───────────────────┬─────────────────────────────────────┘
                    ▼
┌─────────────────────────────────────────────────────────┐
│                LLM (OpenRouter API)                      │
│  - โมเดล: deepseek/deepseek-chat                        │
│  - ประมวลผล Context + คำถาม                             │
│  - ตอบเป็นภาษาไทย/อังกฤษ                                │
└─────────────────────────────────────────────────────────┘
```

---

## 🔧 เทคโนโลยีที่ใช้

### **Frontend (หน้าบ้าน)**
- **Streamlit** - เฟรมเวิร์กสร้างหน้าเว็บ
- **Custom CSS** - สไตล์แบบ ChatGPT
- **ฟอนต์ระบบ** - ฟอนต์มาตรฐานของระบบปฏิบัติการ

### **Backend (หลังบ้าน)**
- **Python 3.8+**
- **Neo4j Graph Database** (bolt://localhost:7687 หรือ Aura)
- **HuggingFace Embeddings** - `paraphrase-multilingual-MiniLM-L12-v2` (384 มิติ)

### **APIs**
- **OpenRouter API** - สำหรับเรียกใช้ LLM (โมเดล DeepSeek)
- ทางเลือก: OpenAI API ที่รองรับ

### **ไลบรารีสำคัญ**
```txt
streamlit>=1.28.0
neo4j>=5.0.0
langchain-huggingface
sentence-transformers
requests
python-dotenv
```

---

## 🚀 ขั้นตอนการติดตั้ง (ทีละขั้น)

### **1. ตั้งค่า Environment**

```bash
# Clone repository จาก GitHub
git clone https://github.com/pheechan/neo4j-network-agent.git
cd neo4j-network-agent

# สร้าง virtual environment
python -m venv .venv

# เปิดใช้งาน virtual environment
# Windows:
.venv\Scripts\activate
# Mac/Linux:
source .venv/bin/activate

# ติดตั้ง dependencies
pip install -r requirements.txt
```

### **2. ติดตั้ง Neo4j**

**ทางเลือก A: Neo4j แบบติดตั้งบนเครื่อง**
```bash
# ดาวน์โหลด Neo4j Desktop จาก https://neo4j.com/download/
# สร้างฐานข้อมูลใหม่
# ตั้งรหัสผ่าน
# เปิดฐานข้อมูลที่ bolt://localhost:7687
```

**ทางเลือก B: Neo4j Aura (บนคลาวด์)**
```bash
# สมัครที่ https://neo4j.com/cloud/aura/
# สร้าง instance ฟรี
# คัดลอก connection URI และ credentials
```

### **3. ตั้งค่าตัวแปร Environment**

สร้างไฟล์ `.env`:
```env
# การตั้งค่า Neo4j
NEO4J_URI=bolt://localhost:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=รหัสผ่านของคุณ
NEO4J_DATABASE=neo4j

# OpenRouter API (สำหรับ LLM)
OPENROUTER_API_KEY=api_key_ของคุณ
OPENROUTER_MODEL=deepseek/deepseek-chat

# การตั้งค่า Vector Search
VECTOR_INDEX_NAME=person_vector_index
VECTOR_NODE_LABEL=Person
VECTOR_SOURCE_PROPERTY=embedding_text
VECTOR_EMBEDDING_PROPERTY=embedding
VECTOR_TOP_K=5
```

### **4. สร้าง Vector Index ใน Neo4j**

รันใน Neo4j Browser หรือ Cypher Shell:
```cypher
CREATE VECTOR INDEX person_vector_index IF NOT EXISTS
FOR (n:Person) ON n.embedding
OPTIONS {indexConfig: {
  `vector.dimensions`: 384,
  `vector.similarity_function`: 'cosine'
}};

-- สร้าง index สำหรับ node types อื่นๆ
CREATE VECTOR INDEX position_vector_index IF NOT EXISTS
FOR (n:Position) ON n.embedding
OPTIONS {indexConfig: {
  `vector.dimensions`: 384,
  `vector.similarity_function`: 'cosine'
}};

CREATE VECTOR INDEX ministry_vector_index IF NOT EXISTS
FOR (n:Ministry) ON n.embedding
OPTIONS {indexConfig: {
  `vector.dimensions`: 384,
  `vector.similarity_function`: 'cosine'
}};

CREATE VECTOR INDEX agency_vector_index IF NOT EXISTS
FOR (n:Agency) ON n.embedding
OPTIONS {indexConfig: {
  `vector.dimensions`: 384,
  `vector.similarity_function`: 'cosine'
}};
```

### **5. สร้าง Embeddings**

```bash
python create_vector_index.py
```

สคริปต์นี้จะ:
- โหลดโมเดล embedding จาก HuggingFace
- สร้าง embeddings สำหรับ nodes ทั้งหมด
- เก็บ embeddings ลงใน Neo4j

### **6. รันแอปพลิเคชัน**

```bash
streamlit run streamlit_app.py
```

เข้าถึงได้ที่: `http://localhost:8501`

---

## 🎨 อธิบายส่วนประกอบของหน้าจอ

### **Sidebar (แถบด้านซ้าย)**
- **สีพื้นหลัง**: `#181818` (เทาเข้ม)
- **หัวข้อ STelligence**
- **ปุ่ม + New Chat** - สร้างบทสนทนาใหม่
- **ประวัติแชท** - รายการบทสนทนาทั้งหมด
- **Settings** - ข้อมูลโมเดลและฐานข้อมูล

### **พื้นที่แชทหลัก**
- **สีพื้นหลัง**: `#212121` (เทากลาง)
- **การ์ดต้อนรับ** - การ์ดไล่สีม่วงเมื่อยังไม่มีข้อความ
- **ข้อความ**:
  - **ข้อความผู้ใช้** - ชิดขวา, พื้นหลัง `#2f2f2f`
  - **ข้อความจากระบบ** - ชิดซ้าย, พื้นหลัง `#1f2937`
- **ช่องพิมพ์ข้อความ** - ด้านล่าง, พื้นหลัง `#303030`

### **ปุ่มต่างๆ**
- **✏️ Edit** - แก้ไขข้อความผู้ใช้และส่งใหม่
- **🔄 Regenerate** - สร้างคำตอบใหม่

---

## 🔍 ระบบค้นหาทำงานอย่างไร

### **1. รับคำถามจากผู้ใช้**
```python
user_input = "Santisook มีความสัมพันธ์กับใครบ้าง"
```

### **2. Vector Search**
- สร้าง embedding สำหรับคำถาม
- ค้นหาในหลาย indexes:
  - `person_vector_index` (30 ผลลัพธ์แรก)
  - `position_vector_index` (30 ผลลัพธ์แรก)
  - `ministry_vector_index` (30 ผลลัพธ์แรก)
  - `agency_vector_index` (30 ผลลัพธ์แรก)

### **3. Graph Traversal**
สำหรับแต่ละผลลัพธ์จาก vector search ให้ดึงความสัมพันธ์:
```cypher
MATCH (n)-[r]->(connected)
RETURN n, r, connected
```

### **4. ตรวจจับเครือข่าย Stelligence**
หากคำถามพูดถึง "Santisook", "Por" หรือ "Knot":
```cypher
MATCH (n:Person)
WHERE n.Stelligence = 'Santisook' OR n.`Connect by` CONTAINS 'Santisook'
RETURN n
```

### **5. สร้าง Context**
จัดรูปแบบผลลัพธ์เป็นข้อความที่มีโครงสร้าง:
```
📊 ข้อมูลจาก Neo4j Knowledge Graph:

👤 Person: อนุทิน ชาญวีรกูล
- ตำแหน่ง: รัฐมนตรีว่าการกระทรวงมหาดไทย
- 👥 ดำรงตำแหน่งโดย: [รายละเอียดตำแหน่ง]
...
```

### **6. ประมวลผลด้วย LLM**
ส่งไปยัง OpenRouter API พร้อม system prompt:
```python
system_prompt = """
คุณเป็นผู้ช่วยตอบคำถามเกี่ยวกับข้าราชการระดับสูงของไทย
ตอบคำถามเป็นภาษาไทยหรืออังกฤษตาม context ที่ให้มา
กฎสำคัญ:
- ห้ามขึ้นต้นด้วยคำนำ
- ใช้ชื่อตำแหน่งเต็ม
- ใช้ bullet points แยกบรรทัด
- ห้ามสร้างข้อมูลเอง
"""
```

---

## 📝 การออกแบบ Prompt

### **โครงสร้าง System Prompt**

1. **กำหนดบทบาท**
   - ผู้ช่วยตอบคำถามเกี่ยวกับข้าราชการไทย
   - รองรับทั้งภาษาไทยและอังกฤษ

2. **กฎสำคัญ**
   - ห้ามขึ้นต้นด้วย "ตามข้อมูล...", "จาก Context..."
   - ใช้ชื่อตำแหน่งเต็ม ("รัฐมนตรีว่าการกระทรวงมหาดไทย" ไม่ใช่ "รัฐมนตรีว่าการ")
   - Bullet points แยกบรรทัด
   - ห้ามสร้างข้อมูลเอง (No hallucination)

3. **ทักษะการวิเคราะห์ข้อมูล**
   - รวมข้อมูลสำหรับคำถามสรุป
   - จัดกลุ่มตาม person/ministry/agency ตามความเหมาะสม
   - แสดงความสัมพันธ์และบริบท

4. **รูปแบบคำตอบ**
   - ตอบหลักก่อน
   - รายละเอียดเสริมด้วย bullets
   - แนะนำคำถามติดตามท้ายสุด

### **ตัวอย่าง Prompt**

**คำตอบที่ดี:**
```
อนุทิน ชาญวีรกูล ดำรงตำแหน่ง:
• รัฐมนตรีว่าการกระทรวงมหาดไทย
• รองนายกรัฐมนตรี

คุณอาจสนใจ:
• ใครเป็นรัฐมนตรีช่วยว่าการกระทรวงมหาดไทย?
```

**คำตอบที่ไม่ดี (หลีกเลี่ยง):**
```
❌ ตามข้อมูลที่ได้รับจาก Knowledge Graph พบว่า อนุทิน ชาญวีรกูล เป็นรัฐมนตรีว่าการ
```

---

## 🧪 คู่มือการทดสอบ

ใช้ `TEST_CASES.md` สำหรับการทดสอบอย่างเป็นระบบ:

### **การทดสอบลำดับความสำคัญ**
1. **Test 1.1**: `อนุทิน ชาญวีรกูล ตำแหน่งอะไร`
   - ต้องแสดงชื่อกระทรวงเต็ม
   - ไม่มีคำนำ

2. **Test 3.1**: `Santisook มีความสัมพันธ์กับใครบ้าง`
   - ควรได้คน 30-50+ คน
   - มีสรุปเครือข่ายด้านบน

3. **Test 7.1**: `รัฐมนตรีว่าการกระทรวงอวกาศคือใคร`
   - ต้องตอบว่า "ไม่พบข้อมูล"
   - ไม่สร้างข้อมูลเอง

### **ระบบให้คะแนน**
- ✅ 5/5 - สมบูรณ์แบบ
- ⚠️ 3-4/5 - ดี (มีข้อบกพร่องเล็กน้อย)
- ❌ 1-2/5 - แย่ (มีปัญหาใหญ่)
- 💥 0/5 - ล้มเหลว

**เป้าหมาย**: คะแนนเฉลี่ย ≥ 4.0/5.0

---

## 🐛 ปัญหาที่พบบ่อยและวิธีแก้

### **ปัญหา 1: Vector Search ไม่พบผลลัพธ์**
**ปัญหา**: `query_with_relationships()` ไม่คืนผลลัพธ์

**วิธีแก้**:
```bash
# ตรวจสอบว่ามี embeddings หรือไม่
MATCH (n:Person) WHERE n.embedding IS NOT NULL RETURN count(n)

# ถ้าได้ 0 ให้รัน:
python create_vector_index.py
```

### **ปัญหา 2: Sidebar ไม่แสดง**
**ปัญหา**: เมนู hamburger ถูกซ่อนด้วย CSS

**วิธีแก้**: ลบ CSS ที่ซ่อน `#MainMenu` และ `header`

### **ปัญหา 3: ปัญหาฟอนต์กับไอคอน**
**ปัญหา**: ไอคอน Material แสดงเป็นข้อความ

**วิธีแก้**: ใช้ฟอนต์มาตรฐานของระบบแทนฟอนต์กำหนดเอง

### **ปัญหา 4: ตอบช้า**
**ปัญหา**: ใช้เวลามากกว่า 10 วินาที

**วิธีแก้**:
- ลด `top_k_per_index` จาก 30 เป็น 10
- เพิ่ม database indexes บน properties ที่ค้นหาบ่อย
- ใช้ Neo4j Aura เพื่อประสิทธิภาพที่ดีขึ้น

### **ปัญหา 5: ภาษาไทยเพี้ยน**
**ปัญหา**: ตัวอักษรไทยแสดงผิดหรือไม่ถูกต้อง

**วิธีแก้**:
- ตรวจสอบให้ทุกไฟล์ใช้ UTF-8 encoding
- ใช้โมเดล embedding ที่รองรับภาษาไทย
- ตรวจสอบ encoding ของฐานข้อมูล Neo4j

---

## 🔒 ข้อควรระวังด้านความปลอดภัย

1. **API Keys**: อย่า commit ไฟล์ `.env` ลง git
2. **ข้อมูล Neo4j**: ใช้ environment variables
3. **Rate Limiting**: ตั้งค่าสำหรับ OpenRouter API
4. **Input Validation**: กรองคำถามจากผู้ใช้
5. **CORS**: ตั้งค่าใน `.streamlit/config.toml`

---

## 📊 ตัวชี้วัดประสิทธิภาพ

### **ประสิทธิภาพปัจจุบัน**
- **เวลาค้นหาทั้งหมด**: 3-8 วินาที (รวม LLM)
- **Vector Search**: 1-2 วินาที
- **Graph Traversal**: 0.5-1 วินาที
- **LLM Response**: 2-5 วินาที

### **เคล็ดลับเพิ่มความเร็ว**
1. Cache คำถามที่ถูกถามบ่อย
2. คำนวณเส้นทางความสัมพันธ์ทั่วไปไว้ล่วงหน้า
3. ใช้ Neo4j APOC procedures สำหรับการค้นหาซับซ้อน
4. ใช้ pagination สำหรับผลลัพธ์

---

## 🚀 การ Deploy

### **Streamlit Cloud**
1. Push โค้ดไป GitHub
2. เชื่อมต่อที่ streamlit.io/cloud
3. ตั้งค่า secrets ใน Streamlit Cloud dashboard
4. Deploy อัตโนมัติเมื่อ push ไป main branch
5. เวลา deployment: 3-5 นาที

### **Environment Variables ใน Streamlit Cloud**
เพิ่มใน Settings → Secrets:
```toml
NEO4J_URI = "bolt://..."
NEO4J_USERNAME = "neo4j"
NEO4J_PASSWORD = "..."
OPENROUTER_API_KEY = "..."
```

---

## 📞 การดูแลรักษา

### **งานบำรุงรักษาหลัก**
1. **รายสัปดาห์**: ตรวจสอบคะแนนจาก test cases
2. **รายเดือน**: อัพเดต embeddings สำหรับข้อมูลใหม่
3. **รายไตรมาส**: ทบทวนและปรับปรุง system prompts
4. **ตามความจำเป็น**: เพิ่ม test cases ใหม่สำหรับกรณีพิเศษ

### **การตรวจสอบ**
- ตรวจสอบ logs ใน Streamlit Cloud
- ติดตามการใช้งาน OpenRouter API
- ตรวจสอบขนาดฐานข้อมูล Neo4j
- รับฟีดแบ็กจากผู้ใช้

---

## 🎓 แหล่งเรียนรู้

### **Neo4j**
- [Neo4j Graph Academy](https://graphacademy.neo4j.com/)
- [ภาษา Cypher Query](https://neo4j.com/docs/cypher-manual/)

### **Vector Search**
- [HuggingFace Embeddings](https://huggingface.co/sentence-transformers)
- [Neo4j Vector Search](https://neo4j.com/docs/cypher-manual/current/indexes-for-vector-search/)

### **Streamlit**
- [เอกสาร Streamlit](https://docs.streamlit.io/)
- [คู่มือ Chat Elements](https://docs.streamlit.io/library/api-reference/chat)

---

## 📄 License และเครดิต

- **โครงการ**: STelligence Network Agent
- **Repository**: github.com/pheechan/neo4j-network-agent
- **พัฒนา**: 2025
- **เทคโนโลยี**: Python, Streamlit, Neo4j, HuggingFace, OpenRouter

---

## 📋 คำสั่งอ้างอิงด่วน

```bash
# ติดตั้ง
python -m venv .venv
.venv\Scripts\activate
pip install -r requirements.txt

# สร้าง embeddings
python create_vector_index.py

# รันแอปบนเครื่อง
streamlit run streamlit_app.py

# Git workflow
git add -A
git commit -m "ข้อความของคุณ"
git push

# ตรวจสอบการเชื่อมต่อ Neo4j
python test_neo4j_conn.py
```

---

**อัพเดตล่าสุด**: 7 พฤศจิกายน 2568
**เวอร์ชัน**: 1.0
**ผู้ดูแล**: Pheemaphat Chan
